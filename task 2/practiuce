/* Global Styles */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
    font-family: Arial, sans-serif;
}

body {
    background-color: #ccc;
    padding: 20px;
}

/* Header */
.header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    background-color: #fff;
    padding: 10px 15px;
    margin-bottom: 20px;
    border-radius: 5px;
    box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
}

.logo {
    font-size: 20px;
    font-weight: bold;
}

nav a {
    margin: 0 10px;
    text-decoration: none;
    color: black;
    font-weight: bold;
}

nav a:hover {
    color: #666;
}

.search {
    display: flex;
    align-items: center;
}

.search input {
    padding: 5px;
    width: 200px;
    margin-right: 10px;
}

.search button {
    background-color: #666;
    color: #fff;
    border: none;
    padding: 5px 15px;
    cursor: pointer;
    border-radius: 4px;
}

/* Main Content */
.content {
    display: flex;
    flex-direction: column;
    gap: 20px;
}

/* Large Movie Poster */
.large-movie {
    display: flex;
    flex-direction: column;
    background-color: #fff;
    border-radius: 5px;
    padding: 15px;
    box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
    width: 100%;
    max-width: 800px;
    margin: 0 auto;
}

.large-movie .poster {
    background-color: #ddd;
    width: 100%;
    height: 300px;
    border-radius: 5px;
    margin-bottom: 15px;
}
.movie-vertical{
    display: grid;
    grid-template-columns: repeat(2, 1fr);
}

/* Centering the Movie Grid */
.movie-grid {
    display: flex;
    justify-content: center; /* Centers the entire grid horizontally */
    align-items: center; /* Centers items vertically if needed */
    gap: 15px;
    margin: 0 auto;
    flex-wrap: wrap;
    max-width: 800px; /* Adjust container width */
}

/* Individual Movie Poster */
.movie-poster {
    background-color: #fff;
    /* grid-template-columns: repeat(2, 1fr); */
    border-radius: 5px;
    padding: 10px;
    box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
    display: flex; /* Flex container for centering */
    flex-direction: column;
    align-items: center; /* Centers the content inside */
    justify-content: center; /* Centers vertically */
    text-align: center; /* Centers text */
    flex: 1 1 calc(33% - 10px); /* Three posters per row */
    min-width: 200px;
    max-width: 250px;
    margin: 0 auto; /* Ensures centering inside the grid */
}

/* Poster Styling */
.movie-poster .poster {
    background-color: #ddd;
    width: 100%; /* Adjust poster size */
    max-width: 200px; /* Constrain the size for consistency */
    height: 300px; /* Poster height */
    border-radius: 5px;
    margin-bottom: 10px;
}





/* Horizontal Movies */
.movie-horizontal {
    display: flex;
    gap: 17px;
    background-color: #fff;
    border-radius: 2px;
    padding: 10px;
    box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
     max-width: 800px;
    margin: 0 auto;
}

.movie-horizontal {
    margin-bottom: 20px; /* Adds spacing between horizontal movies */
}


.movie-horizontal .poster {
    width: 150px;
    height: 200px;
    background-color: #ddd;
    border-radius: 5px;
    flex-shrink: 0;
}

.details {
    flex: 1;
    display: flex;
    flex-direction: column;
    justify-content: space-between;
}

.description {
    font-size: 14px;
    margin-bottom: 10px;
    line-height: 1.4;
}

.watch-button {
    background-color: #666;
    color: white;
    padding: 8px;
    border: none;
    cursor: pointer;
    border-radius: 4px;
    font-size: 14px;
}